  constructor(values = {}) {
{{#if superClass}}
    super();
{{/if}}
    this[_values] = {};
    if (values instanceof {{className}}{{#if superClass}} || values instanceof {{superClass}}{{/if}}) {
      for (const [key, value] of values) {
        this[key] = value;
      }
    } else {
      Object.entries(values).forEach(([key, value]) => this[key] = value);
    }
  }
