{{#if usesArrayProxy}}const ArrayProxy = require('../ArrayProxy');{{/if}}
const { isPresent, _values } = require('../helpers');
{{#if superClass}}

const {{superClass}} = require('./{{superClass}}');
{{/if}}